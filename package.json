{
  "scripts": {
    "test": "which testcafe; testcafe --list-browsers; testcafe -d chrome:headless tests/**/* -r xunit:/tmp/test-results/testcafe/results.xml"
  },
  "devDependencies": {
    "testcafe": "^3.0.1",
    "testcafe-reporter-xunit": "^2.2.3"
  }
}
